# Based on https://github.com/kubernetes-csi/external-attacher/blob/master/Dockerfile
{{- range $key, $value := .CandiVersionMap.k8s }}
  {{- $version := toString $key }}
  {{- if $value.csi.attacher }}
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-{{ $version | replace "." "-" }}
fromImage: common/distroless
import:
  - artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $version | replace "." "-" }}
    add: /csi-attacher
    to: /csi-attacher
    includePaths:
      - csi-attacher
    before: setup
docker:
  ENTRYPOINT: ["/csi-attacher"]
---
artifact: {{ $.ModuleName }}/{{ $.ImageName }}-artifact-{{ $version | replace "." "-" }}
from: {{ $.Images.BASE_GOLANG_20_ALPINE }}
shell:
  beforeInstall:
    - apk add --no-cache make bash git patch rsync gcc musl-dev
    - git clone --depth 1 --branch {{ $value.csi.attacher }} {{ $.SOURCE_REPO }}/kubernetes-csi/external-attacher.git /src
  install:
    - export GO_VERSION=${GOLANG_VERSION}
    - export GOPROXY={{ $.GOPROXY }}
    - cd /src
    {{- if semverCompare "<=1.26" $version }}
    - go get -u golang.org/x/net@v0.17.0
    - go get k8s.io/client-go/tools/clientcmd@v0.26.1
    - go get k8s.io/component-base/metrics@v0.26.0
    - go get google.golang.org/grpc/internal/channelz@v1.52.3
    - go get golang.org/x/net/idna@v0.17.0
    - go mod vendor
    {{- else if semverCompare "=1.27" $version }}
    - go get -u golang.org/x/net@v0.17.0
    - go get k8s.io/client-go/tools/clientcmd@v0.27.1
    - go get k8s.io/component-base/metrics@v0.26.0
    - go get google.golang.org/grpc/internal/channelz@v1.54.0
    - go get golang.org/x/net/idna@v0.17.0
    - go mod vendor
    {{- else if semverCompare "=1.28" $version }}
    - go get -u golang.org/x/net@v0.17.0
    - go get k8s.io/client-go/tools/clientcmd@v0.28.1
    - go get k8s.io/component-base/metrics@v0.28.0
    - go get google.golang.org/grpc/internal/channelz@v1.57.0
    - go get golang.org/x/net/idna@v0.17.0
    - go mod vendor
    {{- end }}
    - make build
    - cp bin/csi-attacher /csi-attacher
    - chown 64535:64535 /csi-attacher
    - chmod 0755 /csi-attacher
  {{- end }}
{{- end }}
